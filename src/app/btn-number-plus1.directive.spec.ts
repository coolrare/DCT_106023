import { BtnNumberPlus1Directive } from './btn-number-plus1.directive';

describe('BtnNumberPlus1Directive', () => {
  it('should create an instance', () => {
    const directive = new BtnNumberPlus1Directive();
    expect(directive).toBeTruthy();
  });
});
